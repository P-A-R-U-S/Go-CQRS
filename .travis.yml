  language: go
  go:
    - "1.10"
  install:
    # Add Godeps dependencies to GOPATH and PATH
    #- export GOPATH="$TRAVIS_BUILD_DIR"
  script:
    - ls
    - pwd
    - echo $GOPATH
    - echo $GOROOT
    - echo $PATH
    - go test -v ./...